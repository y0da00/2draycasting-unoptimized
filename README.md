This draws the rays pixedl by pixel and check each pixel to each obstacle. It runs fine for 800-1000 rays and 10-12 obstacles. It depends on your specs of course but this just unoptimized solution. 
I have another repo on this which solves this better.

Right button to add and remove objects

Hold left button to drag the rays source
